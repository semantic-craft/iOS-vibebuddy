vendor/
.build/
build/
build-hermes/
.swiftpm/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
.DS_Store
.agents/

# Generated by xcodegen (run `xcodegen generate` in each app dir)
VibeBuddyApp/VibeBuddyApp.xcodeproj/
VibeBuddyApp/Sources/Info.plist
VibeBuddyApp/Widget/Info.plist
VibeBuddyApp/Watch/Info.plist
VibeBuddyMacApp/VibeBuddyMacApp.xcodeproj/
VibeBuddyMacApp/Sources/Info.plist

# Session handoffs (kept locally, not tracked)
docs/handoffs/

# Development plans stay on this machine under .scratch/planning/
docs/planning/plan-*.html
docs/planning/status-and-plan-*.md
docs/planning/prompts-*.md
docs/planning/roadmap.md
docs/roadmap-checklist-*.md
docs/finish-checklist-*.md
docs/superpowers/plans/

# Visual-companion brainstorm runtime
.superpowers/

# Python bytecode
__pycache__/

# Build artifacts / release packaging. Everything tools/release-mac.sh writes into
# dist/ is a signed artifact of one specific build (DMG, appcast.xml, deltas, the
# release-note copy Sparkle embeds); it is published to GitHub Releases / gh-pages,
# never committed — a stale committed appcast would point at a DMG nobody has.
build-device/
dist/*
!dist/README.md

# Acceptance screenshots carry real project names; they stay on the machine that
# took them. (Cursor's cloud hand-off once swept them into a public branch.)
docs/qa-screenshots/

# Local agent tooling
.antigravitycli/

.scratch
.claude/

# Local lint / audit artifacts
.brooks-lint-history.json
