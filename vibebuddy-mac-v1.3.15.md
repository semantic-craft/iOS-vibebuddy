# VibeBuddy 1.3.15 — macOS

## 中文

- Mac 任务详情改为独立阅读区与右侧工具架：Changes、Output、Activity 按需打开，支持标签切换、放大和 `⌥⌘B` 收起／恢复。窄窗口打开工具时占用详情区，点击「Session」返回。
- 输入区固定在阅读区底部。草稿按会话保存，查看或放大工具、返回正文、切换任务都不会丢失或串到另一会话；发送后清空对应草稿。
- 会话跳转和通知设置集中到标题栏；宽窗口可并排阅读请求与文件变更，审批操作留在阅读区底部。
- 文件变更使用统一表头与只读 diff，统计无法读取时显示不可用，不再当作零改动。Output 保留来源与有限摘录说明。
- 任务行以 agent 图标开头，并保留状态提示；Needs you 筛选包含确认失败的任务。统一 Mac 界面的 VibeBuddy 品牌大小写。
- 同步已合入的共享待处理导航与按所显示结果轮次确认已读的改进。

## English

- A dedicated reader and collapsible right-hand tools for Changes, Output and Activity, with tabs, expansion and Option-Command-B. Narrow windows show tools across the detail area with a Session return action.
- A fixed composer with separate per-session drafts that survive tool navigation and session switching. Sending clears the corresponding draft.
- Consolidated session actions, an updated read-only changes view, explicit unavailable statistics, and attributed output excerpts.
- Agent icons lead task rows; the Needs you filter includes confirmed failures. Mac branding is consistently capitalized.
- Includes shared pending navigation and acknowledgements bound to the displayed completion.

## 范围与限制 / Scope and limits

- 本次发布 macOS 直接分发版；不包含 iPhone／Watch 安装或 App Store 提交。
- 窄窗口的全宽工具会暂时隐藏阅读区和审批键，返回 Session 后恢复。
- 界面点击验证与真实 agent 数据验收分别记录；演示实例的点击通过不代表真实审批发送、语音或实体设备闭环已验收。
- This release distributes the Mac app. Physical-device, voice and approval-delivery acceptance remain separate from UI navigation checks.
