# 05: 增加隐私受控的生命周期事件日志

**What to build:** VibeBuddy 保存一个容量有上限的本地生命周期日志，用于重启恢复、解释状态来源和诊断漏报；日志只记录规范化状态变化与必要的来源元数据，默认不保留完整 prompt、推理、消息正文或工具输出。

**Blocked by:** 02: 显示观察来源与兼容性健康状态.

**Status:** ready-for-agent

- [x] 每条记录包含会话身份、代理、规范化事件、观察来源、时间和归约结果，但默认不写入完整用户/助手内容或工具输入输出。
- [x] 日志按明确的容量或保留期自动轮转，损坏、升级中断或磁盘不可写时监控继续 fail-open 运行。
- [x] daemon 重启后仅恢复仍有意义的活动/等待状态，不重放旧完成通知，也不让已结束会话重新出现。
- [x] 设置或诊断界面能查看经过脱敏的近期时间线并清除日志；仅用少量快速回归测试保护轮转、隐私字段和恢复主路径，并以实际 daemon 重启、损坏/不可写模拟完成端到端验收。

## Comments

- 实现来源提交链：`d710bfd` → `04f9986`；独立复审先发现 0600 发布窗口、恢复窗口配置顺序和清除失败不可重试，修复后同一 reviewer 与集成复审均通过。
- Journal 限制为 7 天 / 250 条；目录为 0700，同目录 staging 从创建即 0600，write、fsync 与权限复核成功后才原子 rename，失败保留旧目标并清理 staging。
- 集成态 focused tests 27/27、Mac App Debug 构建通过。真实隔离 daemon 已覆盖当前 hook/rollout、重启只恢复 active/wait、done/SessionEnd 不复活、损坏与不可写 fail-open、清除失败后权限修复重试，以及磁盘敏感正文扫描。
- 正式 `/Applications/VibeBuddyMacApp.app` 全程未退出或替换；受单实例机制影响，没有把 Settings 时间线的第二实例视觉附着误报为已完成。
