name: VibeBuddyApp
options:
  bundleIdPrefix: com.vibebuddy
  deploymentTarget:
    iOS: "17.0"
    watchOS: "26.6"
  createIntermediateGroups: true
  developmentLanguage: en

packages:
  VibeBuddyKit:
    path: ../VibeBuddyKit
targets:
  VibeBuddyApp:
    type: application
    platform: iOS
    sources:
      - path: Sources
      - path: Shared
      - path: Resources
    dependencies:
      - package: VibeBuddyKit
        product: VibeBuddyKit
      - target: VibeBuddyWidget
      - target: VibeBuddyWatch
    info:
      path: Sources/Info.plist
      properties:
        CFBundleDisplayName: vibebuddy
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        UILaunchScreen: {}
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        NSCameraUsageDescription: "Scan the pairing QR shown by the vibebuddy Mac app."
        NSLocalNetworkUsageDescription: "Connect to the vibebuddy Mac app on your local network to show live session status and approve actions."
        NSMicrophoneUsageDescription: "Talk to your agents by voice. With the optional voice companion, your microphone audio and selected session context are sent directly to the AI provider you choose (OpenAI, Google, or Alibaba) using your own API key — they never pass through a vibebuddy server."
        NSSupportsLiveActivities: true
        ITSAppUsesNonExemptEncryption: false
        CFBundleURLTypes:
          - CFBundleURLName: com.vibebuddy.app.deeplink
            CFBundleURLSchemes:
              - vibebuddy
        NSAppTransportSecurity:
          NSAllowsLocalNetworking: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.vibebuddy.app
        MARKETING_VERSION: "1.3"
        CURRENT_PROJECT_VERSION: "9"
        SWIFT_VERSION: "6.0"
        TARGETED_DEVICE_FAMILY: "1"
        DEVELOPMENT_TEAM: LQAVR62TK2
        CODE_SIGN_STYLE: Automatic
        # Time Sensitive (Q23) lives in the entitlements files:
        # com.apple.developer.usernotifications.time-sensitive
        CODE_SIGN_ENTITLEMENTS: VibeBuddyApp.entitlements
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
      configs:
        # Release/Archive (App Store, TestFlight) uses production APNs; local
        # Debug runs on device use the sandbox (development) environment.
        Debug:
          CODE_SIGN_ENTITLEMENTS: VibeBuddyApp-Debug.entitlements
  VibeBuddyWidget:
    type: app-extension
    platform: iOS
    sources:
      - path: Widget
      - path: Shared
    dependencies:
      - package: VibeBuddyKit
        product: VibeBuddyKit
    info:
      path: Widget/Info.plist
      properties:
        CFBundleDisplayName: vibebuddy
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.vibebuddy.app.widget
        MARKETING_VERSION: "1.3"
        CURRENT_PROJECT_VERSION: "9"
        SWIFT_VERSION: "6.0"
        TARGETED_DEVICE_FAMILY: "1"
        DEVELOPMENT_TEAM: LQAVR62TK2
        CODE_SIGN_STYLE: Automatic
        CODE_SIGN_ENTITLEMENTS: Widget/VibeBuddyWidget.entitlements
  VibeBuddyWatch:
    type: application
    platform: watchOS
    sources:
      - path: Watch
      - path: WatchShared
    dependencies:
      - target: VibeBuddyWatchWidget
      - package: VibeBuddyKit
        product: VibeBuddyKit
    info:
      path: Watch/Info.plist
      properties:
        CFBundleDisplayName: vibebuddy
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        WKApplication: true
        WKCompanionAppBundleIdentifier: com.vibebuddy.app
        WKRunsIndependentlyOfCompanionApp: false
        # iOS's Watch app only lists a third-party app in its complication
        # configuration UI (and fetches its icon) when the watch reports a
        # ClockKit principal class. The class is an empty data source; every
        # real complication is WidgetKit. See .scratch/watch-quota/acceptance/
        # defect-quota-configuration-icon.md.
        CLKComplicationPrincipalClass: $(PRODUCT_MODULE_NAME).ComplicationController
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.vibebuddy.app.watchkitapp
        CODE_SIGN_ENTITLEMENTS: Watch/VibeBuddyWatch.entitlements
        MARKETING_VERSION: "1.3"
        CURRENT_PROJECT_VERSION: "9"
        SWIFT_VERSION: "6.0"
        TARGETED_DEVICE_FAMILY: "4"
        DEVELOPMENT_TEAM: LQAVR62TK2
        CODE_SIGN_STYLE: Automatic
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
  VibeBuddyWatchWidget:
    type: app-extension
    platform: watchOS
    sources:
      - path: WatchWidget
      - path: WatchShared
      - path: Watch/Assets.xcassets
    dependencies:
      - package: VibeBuddyKit
        product: VibeBuddyKit
    info:
      path: WatchWidget/Info.plist
      properties:
        CFBundleDisplayName: vibebuddy
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.vibebuddy.app.watchkitapp.widget
        MARKETING_VERSION: "1.3"
        CURRENT_PROJECT_VERSION: "9"
        SWIFT_VERSION: "6.0"
        TARGETED_DEVICE_FAMILY: "4"
        DEVELOPMENT_TEAM: LQAVR62TK2
        CODE_SIGN_STYLE: Automatic
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        CODE_SIGN_ENTITLEMENTS: WatchWidget/VibeBuddyWatchWidget.entitlements
  VibeBuddyAppTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: Tests
    dependencies:
      - target: VibeBuddyApp
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.vibebuddy.app.tests
        GENERATE_INFOPLIST_FILE: YES
        SWIFT_VERSION: "6.0"
